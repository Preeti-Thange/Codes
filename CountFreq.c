#include<stdio.h>
#include<stdlib.h>

int CountFreq(int Arr[], int iLength, int val)
{
    int i=0,iCnt=0;
    for(i=0; i<iLength; i++)
    {
        if(Arr[i]== val)
        {
            break;
        }
    }
}
